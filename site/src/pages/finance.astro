---
/**
 * `/finance` - il Portfolio Manager (divisione Finance).
 *
 * E' la landing che fino a S11 stava sulla radice: stesso componente, stesse
 * sezioni, nessuna riscrittura. Cambia solo l'indirizzo, perche' la radice ora
 * appartiene allo studio e non a uno dei due prodotti.
 */
import it from '../i18n/it';
import LandingPage from '../components/LandingPage.astro';
---
<LandingPage t={it} lang="it" />
