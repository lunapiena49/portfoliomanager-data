---
/**
 * `/games` - ROLLQUEST (divisione Games). In sviluppo: nessuno store, nessuna
 * data. V. il commento in testa a GamesPage.astro.
 */
import it from '../i18n/it';
import GamesPage from '../components/GamesPage.astro';
---
<GamesPage t={it} lang="it" />
